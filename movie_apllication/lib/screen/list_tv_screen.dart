import 'package:flutter/material.dart';

class ListTvScreen extends StatelessWidget {
  static const routename = "/tv_list";
  const ListTvScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.blue,
      ),
    );
  }
}
